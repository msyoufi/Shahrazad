interface ConfirmationDialogData {
  title: string,
  message: string,
  actionButton: string
}

interface Profile {
  name: string,
  bio_html: string,
  email: string,
  hero_html: string,
  profileImageUrl: string,
  coverImageUrl: string
}