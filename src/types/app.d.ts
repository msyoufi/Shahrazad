interface ConfirmationDialogData {
  title: string,
  message: string,
  actionButton: string
}

interface Profile {
  name: string,
  bio: string,
  profileImageUrl: string,
  coverImageUrl: string,
}